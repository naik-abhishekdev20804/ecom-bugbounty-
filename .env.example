# Optional: if the UI is hosted on a different origin than the API
# VITE_API_BASE=https://your-api.example.com

# Local API (npm run dev)
# API_PORT=8787

# Production Node server (npm start after npm run build)
# PORT=4173
